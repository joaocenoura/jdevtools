#!/bin/bash
set -e
source {{ scripts_dir }}/functions.sh

###############################################################################
# restore jenkins home directory (ensure jenkins service is stopped)
restore_tar {{ backup_dir_jenkins }} {{ backup_filename_jenkins }}